<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Catálogo de Juegos de Mesa</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        body {
            background: #f8f9fa;
            font-family: 'Segoe UI', sans-serif;
        }

        .card {
            border: none;
            border-radius: 16px;
            transition: transform 0.3s ease;
        }

            .card:hover {
                transform: scale(1.03);
                box-shadow: 0 8px 20px rgba(0,0,0,0.1);
            }

        .btn-primary {
            border-radius: 50px;
            font-weight: bold;
        }

        .modal-content {
            border-radius: 16px;
        }

        .modal-header {
            background: #6f42c1;
            color: white;
            border-bottom: none;
            border-top-left-radius: 16px;
            border-top-right-radius: 16px;
        }

        .modal-body {
            background-color: #fdfdfe;
        }
    </style>
</head>
<body>

    <div class="container py-5">
        <h1 class="text-center mb-5 text-primary">🎲 Catálogo de Juegos de Mesa</h1>

        <div class="row justify-content-center">

            <!-- Juego de mesa -->
            <div class="col-md-6 col-lg-4">
                <div class="card text-center">
                    <img src="https://via.placeholder.com/300x200?text=Juego+de+Mesa" class="card-img-top" alt="Juego de Mesa">
                    <div class="card-body">
                        <h4 class="card-title text-dark">Aventura Mágica</h4>
                        <p class="text-muted">Un juego de mesa épico para toda la familia.</p>
                        <button class="btn btn-primary px-4 py-2" data-bs-toggle="modal" data-bs-target="#comprarJuego">
                            <i class="bi bi-cart-fill me-2"></i>Comprar
                        </button>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <!-- Modal para comprar el juego -->
    <div class="modal fade" id="comprarJuego" tabindex="-1" aria-labelledby="comprarJuegoLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content shadow-lg">
                <div class="modal-header">
                    <h5 class="modal-title" id="comprarJuegoLabel">🛒 Comprar "Aventura Mágica"</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                </div>
                <div class="modal-body">
                    <p class="mb-4">Rellena tus datos para completar tu compra:</p>
                    <form>
                        <div class="mb-3">
                            <label class="form-label">Nombre completo</label>
                            <input type="text" class="form-control" placeholder="Tu nombre" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Correo electrónico</label>
                            <input type="email" class="form-control" placeholder="tucorreo@example.com" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Método de pago</label>
                            <select class="form-select" required>
                                <option selected disabled>Selecciona uno</option>
                                <option>Tarjeta de crédito</option>
                                <option>PayPal</option>
                                <option>Transferencia bancaria</option>
                            </select>
                        </div>
                        <button type="submit" class="btn btn-success w-100 py-2">
                            <i class="bi bi-check-circle-fill me-1"></i>Confirmar compra
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
